# Egypt Museum Experience

Expo Router app for a premium museum visit experience: tickets, indoor map, guided routes and treasure hunts.

## Run locally

```bash
npm install
npm run start-web
```

## Vercel

The project is configured for Vercel with:

```bash
npm run build
```

The static export is generated in `dist`.
